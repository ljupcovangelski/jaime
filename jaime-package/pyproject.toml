[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "jaime-package"
version = "0.0.1"
description = "Shared library for Jaime — Juju AI Medic Engine"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["jaime*"]
