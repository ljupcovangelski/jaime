[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["charms/machine/tests"]
pythonpath = ["charms/machine/src", "jaime-package"]

[tool.coverage.run]
source = ["charms/machine/src", "jaime-package"]
